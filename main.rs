fn main() {
    // First, Rust style is to indent with four spaces, not a tab.
    // Second, `println!` calls a Rust macro. If it called a function instead, it would be entered as `println` (without the `!`).
    println!("Hello, world!");
}
